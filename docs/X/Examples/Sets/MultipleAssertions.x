

let S be {...}
let T be {...}

let a in S
# S => {a, ...}

# Throws error
let a in T

|- a (- T

# Need two types of SetElement:
#   - Elements which represent `Some member of a set` and cannot be added to other sets or referenced in their own right
#   - Elements which represent themselves and can be added to sets